spring:
  # Схему создают Flyway-миграции (ddl-auto=none), как в продакшене
  # (V1–V10: партиционированные messages, GIN-индексы FTS), а не
  # автогенерация Hibernate.
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none